import 'package:flutter/material.dart';

class BayansScreen extends StatelessWidget {
  const BayansScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Bayas'),
      ),
    );
  }
}
